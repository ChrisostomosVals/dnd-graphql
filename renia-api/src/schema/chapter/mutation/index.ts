import { createChapter } from "./createChapter";
import { deleteChapter } from "./deleteChapter";
import { updateChapter } from "./updateChapter";

export const ChapterMutation = {
    createChapter,
    updateChapter,
    deleteChapter
}